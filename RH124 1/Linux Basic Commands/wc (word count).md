Displays number of lines, words, characters in a file.

·      Common Options:

·      **wc -l file** → Count lines

·      **wc -w file** → Count words

·      **wc -c file** → Count bytes

·      **wc -m file** → Count characters

·      **wc -L file** → Length of longest line